# Default loop prompt — Quant Scout

Run the `/quant-scout` command to scout the quant world for new research,
techniques, skills, and resources, then write and commit a dated digest.

Use:

    /quant-scout

If the most recent digest in `research/digests/` is already dated today, skip this
iteration (no duplicate digest for the same day) and report that it was skipped.
